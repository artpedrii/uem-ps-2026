#ifndef PAR_H
#define PAR_H

// definição de tipos

typedef struct {
  int a;
  int b;
} Par;

// definição de protótipos de funções

Par soma_par(Par x, Par y);

void mostra_par(Par z);

#endif /* MAP_H */
